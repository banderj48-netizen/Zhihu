# Blender 便携版安装目录与安装包（体积过大，不入库）
blender-portable/
blender-4.5.10-windows-x64.zip

# 本地模型密钥环境，不提交真实凭据
backend/.env.models

# Blender 自动备份文件
*.blend1

# 前端依赖与 Next.js 构建产物：可通过 npm install / npm run build 重新生成，不提交仓库。
frontend/node_modules/
frontend/.next/
